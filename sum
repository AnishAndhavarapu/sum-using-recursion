#include<stdio.h>
int sum(int n)
{
	if(n==0)
	return 0;
	else
    return (n%10+sum(n/10));
}
int main()
{
	int a;
	printf("enter a number:");
	scanf("%d",&a);
	int r=sum(a);
printf("sum of numbers is:%d",r);
return 0;
}
